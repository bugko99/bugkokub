FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package*.json ./
RUN npm install

# Source code will be mounted via bind mount
# node_modules will use anonymous volume

EXPOSE 5173

CMD ["npm", "run", "dev"]
